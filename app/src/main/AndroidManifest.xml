<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- App discovery -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Network -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Installation & Updates -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.ENFORCE_UPDATE_OWNERSHIP" />

    <!-- Foreground Services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Battery & Wake Lock -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Storage: Only for custom icon/header creation -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Notifications: Required on Android 13+ (API 33) to post update notifications.
         On older versions the permission is granted automatically. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name="app.morphe.manager.ManagerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="false"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.MorpheManager"
        tools:targetApi="34">

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- Main Activity without launcher -->
        <activity
            android:name="app.morphe.manager.MainActivity"
            android:exported="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboard|keyboardHidden"
            android:theme="@style/Theme.MorpheManager">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Default launcher icon -->
        <activity-alias
            android:name="app.morphe.manager.MainActivity_Default"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:targetActivity="app.morphe.manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Alternative launcher icons -->
        <activity-alias
            android:name="app.morphe.manager.MainActivity_Light_2"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_light_2"
            android:roundIcon="@mipmap/ic_launcher_light_2"
            android:label="@string/app_name"
            android:targetActivity="app.morphe.manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name="app.morphe.manager.MainActivity_Light_3"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_light_3"
            android:roundIcon="@mipmap/ic_launcher_light_3"
            android:label="@string/app_name"
            android:targetActivity="app.morphe.manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name="app.morphe.manager.MainActivity_Dark_1"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_dark_1"
            android:roundIcon="@mipmap/ic_launcher_dark_1"
            android:label="@string/app_name"
            android:targetActivity="app.morphe.manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name="app.morphe.manager.MainActivity_Dark_2"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_dark_2"
            android:roundIcon="@mipmap/ic_launcher_dark_2"
            android:label="@string/app_name"
            android:targetActivity="app.morphe.manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name="app.morphe.manager.MainActivity_Dark_3"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_dark_3"
            android:roundIcon="@mipmap/ic_launcher_dark_3"
            android:label="@string/app_name"
            android:targetActivity="app.morphe.manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <service android:name="app.morphe.manager.service.InstallService" />
        <service android:name="app.morphe.manager.service.UninstallService" />

        <receiver
            android:name="app.morphe.manager.receiver.InstallReceiver"
            android:exported="false" />
        <receiver
            android:name="app.morphe.manager.receiver.UninstallReceiver"
            android:exported="false" />

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false"
            tools:node="merge">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="patching" />
        </service>

        <provider
            android:name="app.morphe.manager.domain.installer.InstallerFileProvider"
            android:authorities="${applicationId}.installerfileprovider"
            android:exported="false"
            android:grantUriPermissions="true" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Firebase Cloud Messaging — receives push notifications bypassing Doze mode -->
        <service
            android:name="app.morphe.manager.service.MorpheFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Disable FCM auto-init so we only register when user opts in to notifications -->
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

    </application>
</manifest>
